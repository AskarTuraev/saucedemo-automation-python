# 🚀 Быстрый старт - Applitools Visual Testing

Краткое руководство для запуска визуального тестирования SauceDemo с Applitools Eyes.

## ⚡ Минимальная настройка (5 минут)

### 1. Установка зависимостей

```powershell
# Активируйте виртуальное окружение (если еще не активировано)
.\venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Установите браузеры Playwright
playwright install chromium
```

### 2. Получите Applitools API ключ

1. Зарегистрируйтесь на https://applitools.com/ (бесплатный аккаунт)
2. Войдите в аккаунт
3. Скопируйте API ключ из профиля

### 3. Создайте файл `.env`

```powershell
# Скопируйте пример конфигурации
copy .env.example .env

# Откройте .env в блокноте и вставьте ваш API ключ
notepad .env
```

В файле `.env` замените `your_api_key_here` на ваш реальный API ключ:
```
APPLITOOLS_API_KEY=ваш_реальный_api_ключ_здесь
```

## 🎯 Запуск тестов

### Шаг 1: Создание Baseline (первый запуск)

```powershell
python -m pytest tests/test_saucedemo_baseline.py -v
```

**Что произойдет:**
- Откроется браузер
- Тест пройдет авторизацию → каталог → корзину → checkout
- Создастся 4 визуальных снимка в Applitools
- Тест завершится успешно (PASSED)

**Время выполнения:** ~30-60 секунд

### Шаг 2: Запуск теста с дефектами (второй запуск)

```powershell
python -m pytest tests/test_saucedemo_visual_defects.py -v
```

**Что произойдет:**
- Откроется браузер
- Тест внесет 4 визуальных дефекта
- Applitools обнаружит отличия от baseline
- Результаты будут в статусе "Unresolved"

**Время выполнения:** ~30-60 секунд

## 📊 Просмотр результатов

1. Откройте https://eyes.applitools.com/
2. Войдите в ваш аккаунт
3. Найдите батч "SauceDemo Visual Regression Tests"
4. Посмотрите обнаруженные визуальные различия

## 🎨 Обнаруженные дефекты

После второго запуска вы увидите в Applitools:

1. **Login Page:** Кнопка Login исчезла
2. **Inventory Page:** Красный фон + скрытая цена
3. **Cart Page:** Зеленая кнопка + сдвинутый layout
4. **Checkout Page:** Скрытое поле + маленькая кнопка

## 💡 Полезные команды

```powershell
# Запуск только baseline теста
python -m pytest -m baseline -v

# Запуск только теста с дефектами
python -m pytest -m visual_defects -v

# Запуск всех тестов
python -m pytest -v

# Запуск с HTML отчетом
python -m pytest -v --html=reports/report.html --self-contained-html

# Запуск в headless режиме (быстрее)
# Измените в .env: HEADLESS=true
```

## ❓ Проблемы?

### "APPLITOOLS_API_KEY не установлен"
- Проверьте что файл `.env` создан
- Убедитесь что API ключ скопирован правильно

### "playwright not found"
- Выполните: `playwright install`

### Тест падает с ошибкой
- Убедитесь что сайт https://www.saucedemo.com/ доступен
- Проверьте интернет-соединение

## 📚 Дальнейшие шаги

Подробная документация в [README.md](README.md)

**Готово! Теперь вы можете использовать Applitools для визуального регрессионного тестирования.**
