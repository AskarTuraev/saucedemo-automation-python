# SauceDemo Test Automation Framework

Проект автоматизации тестирования для веб-сайта [SauceDemo](https://www.saucedemo.com/). Реализован на **Python** с использованием **Selenium WebDriver**, **Pytest** и паттерна **Page Object Model (POM)**.

## 📂 Структура проекта

```text
saucedemo_automation/
├── config/             # Конфигурационные файлы
├── logs/               # Логи выполнения (создается автоматически)
├── pages/              # Page Objects (классы страниц)
│   ├── base_page.py    # Базовые методы Selenium
│   ├── login_page.py   # Страница входа
│   ├── inventory_page.py # Каталог
│   ├── cart_page.py    # Корзина
│   └── checkout_page.py# Оформление заказа
├── tests/              # Тесты
│   ├── conftest.py     # Фикстуры (драйвер, скриншоты)
│   └── test_e2e.py     # Основные сценарии
├── utils/              # Утилиты (логгер)
├── reports/            # Отчеты Allure
├── requirements.txt    # Зависимости
├── pytest.ini          # Настройки Pytest
└── README.md           # Эта инструкция
```

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone git@github.com:AskarTuraev/saucedemo-automation-python.git
cd saucedemo_automation
```

### 2. Настройка окружения (Windows)
Создайте виртуальное окружение и активируйте его:
```powershell
python -m venv venv
.\venv\Scripts\activate
```

### 3. Установка зависимостей
```powershell
pip install -r requirements.txt
```

---

## ▶️ Запуск тестов

**Важно:** Запускайте тесты как модуль Python, чтобы избежать ошибок импорта (`ModuleNotFoundError`).

### Базовый запуск (все тесты)
```powershell
python -m pytest
```

### Запуск с просмотром в браузере (не headless)
По умолчанию в `conftest.py` браузер запускается в видимом режиме.

### Запуск конкретного теста
```powershell
python -m pytest tests/test_e2e.py::test_login_success
```

---

## 📊 Отчеты Allure

Тесты автоматически сохраняют результаты в папку `reports/allure-results`.

Чтобы просмотреть красивый HTML-отчет:

1.  Убедитесь, что установлен [Allure Commandline](https://github.com/allure-framework/allure2/releases) (или `scoop install allure`).
2.  Выполните команду:
    ```powershell
    allure serve reports/allure-results
    ```

---

## 🛠 Настройки VS Code

Для комфортной работы в VS Code в папке `.vscode/settings.json` уже прописаны настройки:
*   Автоматическое обнаружение виртуального окружения `venv`.
*   Включение панели тестирования (колба слева).

Если VS Code не видит тесты:
1.  Нажмите `Ctrl + Shift + P`.
2.  Выберите `Python: Select Interpreter`.
3.  Укажите путь: `./venv/Scripts/python.exe`.

## 🧪 Учетные данные

Тесты используют стандартного пользователя:
*   **User:** `standard_user`
*   **Pass:** `secret_sauce`