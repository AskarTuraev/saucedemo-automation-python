# SauceDemo Test Automation Framework

Проект автоматизации тестирования для веб-сайта [SauceDemo](https://www.saucedemo.com/). Реализован на **Python** с использованием **Selenium WebDriver**, **Pytest** и паттерна **Page Object Model (POM)**.

## 📂 Структура проекта

```text
saucedemo_automation/
├── config/             # Конфигурационные файлы
├── logs/               # Логи выполнения (создается автоматически)
├── pages/              # Page Objects (классы страниц)
│   ├── base_page.py    # Базовые методы Selenium
│   ├── login_page.py   # Страница входа
│   ├── inventory_page.py # Каталог
│   ├── cart_page.py    # Корзина
│   └── checkout_page.py# Оформление заказа
├── tests/              # Тесты
│   ├── conftest.py     # Фикстуры (драйвер, скриншоты)
│   └── test_e2e.py     # Основные сценарии
├── utils/              # Утилиты (логгер)
├── reports/            # Отчеты Allure
├── requirements.txt    # Зависимости
├── pytest.ini          # Настройки Pytest
└── README.md           # Эта инструкция
```

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone git@github.com:AskarTuraev/saucedemo-automation-python.git
cd saucedemo_automation
```

### 2. Настройка окружения (Windows)
Создайте виртуальное окружение и активируйте его:
```powershell
python -m venv venv
.\venv\Scripts\activate
```

### 3. Установка зависимостей
```powershell
pip install -r requirements.txt
```

---

## ▶️ Запуск тестов

**Важно:** Запускайте тесты как модуль Python, чтобы избежать ошибок импорта (`ModuleNotFoundError`).

### Базовый запуск (все тесты)
```powershell
python -m pytest
```

### Запуск с просмотром в браузере (не headless)
По умолчанию в `conftest.py` браузер запускается в видимом режиме.

### Запуск конкретного теста
```powershell
python -m pytest tests/test_e2e.py::test_login_success
```

---

## 📊 Отчеты Allure

Тесты автоматически сохраняют результаты в папку `reports/allure-results`.

Чтобы просмотреть красивый HTML-отчет:

1.  Убедитесь, что установлен [Allure Commandline](https://github.com/allure-framework/allure2/releases) (или `scoop install allure`).
2.  Выполните команду:
    ```powershell
    allure serve reports/allure-results
    ```

---

## 🛠 Настройки VS Code

Для комфортной работы в VS Code в папке `.vscode/settings.json` уже прописаны настройки:
*   Автоматическое обнаружение виртуального окружения `venv`.
*   Включение панели тестирования (колба слева).

Если VS Code не видит тесты:
1.  Нажмите `Ctrl + Shift + P`.
2.  Выберите `Python: Select Interpreter`.
3.  Укажите путь: `./venv/Scripts/python.exe`.

## 🧪 Учетные данные

Тесты используют стандартного пользователя:
*   **User:** `standard_user`
*   **Pass:** `secret_sauce`

---

## 🤖 AI Code Reviewer (Ollama / Gemma 3)

В проект интегрирован помощник, работающий через локальную нейросеть **Ollama** (по умолчанию модель **gemma3**). Он анализирует ваш код на предмет качества, стабильности тестов и соблюдения архитектурных паттернов, не отправляя данные в облако.

### Настройка AI ассистента:

1.  **Установите Ollama:** Скачайте с [ollama.com](https://ollama.com/).
2.  **Загрузите модель:** Выполните в терминале `ollama pull gemma3`.
3.  **Установите зависимости:**
    ```powershell
    pip install -r code_reviewer/requirements.txt
    ```

### Как пользоваться:
1.  Выделите любой фрагмент кода в редакторе VS Code.
2.  Нажмите `Ctrl + Shift + P` и выберите задачу **"Run Task: Local Code Reviewer"**.
3.  (Рекомендуется) Добавьте горячую клавишу `Ctrl + Alt + R` (инструкция в `code_reviewer/README.md`).

Подробные инструкции по смене модели находятся в `code_reviewer/README.md`.

### 🌍 Использование в других проектах

Этот модуль является полностью переносимым. Вы можете добавить AI-ревью в любой свой проект:

1.  **Копирование:** Скопируйте папку `code_reviewer` в корень вашего проекта.
2.  **Настройка:** Повторите шаги настройки (создание `.env` и установка `requirements.txt`) в новом проекте.
3.  **Интеграция:**
    *   **Вариант А (Простой):** Скопируйте папку `.vscode` (или добавьте задачу в существующий `tasks.json`), чтобы запускать ревью через "Run Task".
    *   **Вариант Б (Расширение):** Используйте локальное расширение из папки `code_reviewer/vscode_extension` для добавления кнопки в контекстное меню (подробнее в `code_reviewer/vscode_extension/README.md`).