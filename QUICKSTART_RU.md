# 🚀 Быстрый старт - AI-Driven QA Pipeline

**Полная инструкция на русском для запуска за 5 минут**

---

## ⚡ Супер быстрый старт (3 команды)

```bash
# 1. Установка всего необходимого
setup.bat

# 2. Создайте .env файл с вашим OpenAI API ключом
# Скопируйте .env.example в .env и добавьте ключ

# 3. Запустите quick test
quick_test.bat
```

**Готово! Вы только что сгенерировали и запустили автотесты с помощью AI! 🎉**

---

## 📋 Детальная инструкция

### Шаг 1: Подготовка окружения

```bash
# Запустите скрипт установки
setup.bat
```

**Этот скрипт автоматически:**
- ✅ Проверит Python
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Установит Playwright браузеры
- ✅ Установит Spacy модель для PII

### Шаг 2: Получите OpenAI API ключ

1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **API Keys**
4. Нажмите **Create new secret key**
5. Скопируйте ключ (выглядит как `sk-...`)

### Шаг 3: Настройте .env файл

Создайте файл `.env` в корне проекта:

```env
# Обязательно
OPENAI_API_KEY=sk-ваш-ключ-здесь

# Опционально (для Applitools тестов)
APPLITOOLS_API_KEY=ваш-ключ

# Настройки приложения
BASE_URL=https://www.saucedemo.com
BROWSER=chromium
HEADLESS=false
```

### Шаг 4: Запустите тесты!

#### Вариант А: Быстрый тест (рекомендуется для начала)

```bash
quick_test.bat
```

**Что произойдет:**
1. Создаются тестовые требования
2. AI генерирует тест-сценарии (GPT-4)
3. Создается Playwright код
4. Тесты запускаются в браузере
5. Вы видите результаты!

**Время выполнения:** 2-3 минуты

#### Вариант Б: Тест PII Detection

```bash
test_pii.bat
```

**Что произойдет:**
1. Создается файл с чувствительными данными (email, API ключи и т.д.)
2. AI находит и маскирует PII
3. Показывает оригинал vs безопасную версию

**Время выполнения:** 5-10 секунд

---

## 🎯 Что делать дальше?

### 1. Посмотрите сгенерированные тесты

```bash
cd quick_tests
dir

# Откройте любой тест
type test_login_with_valid_credentials.py
```

### 2. Запустите AI Code Review

```bash
# Вернитесь в корневую папку
cd ..

# Запустите review
python -m ai_qa_pipeline.modules.code_review.cli full quick_tests --llm openai -o review.md

# Посмотрите результаты
type review.md
```

### 3. Попробуйте с вашими требованиями

**Создайте файл my_requirements.txt:**

```text
Feature: Добавление товара в корзину

Как пользователь
Я хочу добавлять товары в корзину
Чтобы потом их купить

Acceptance Criteria:
- Пользователь видит кнопку "Add to cart"
- При нажатии товар добавляется в корзину
- Счетчик корзины увеличивается
- Кнопка меняется на "Remove"
```

**Запустите генерацию:**

```bash
python -m ai_qa_pipeline.modules.code_generation.cli full my_requirements.txt --base-url https://www.saucedemo.com --llm openai --output my_tests

cd my_tests
pytest -v --headed
```

---

## 🎬 Демонстрация для защиты проекта

### Demo Script (5 минут):

```bash
# 1. Покажите PII Protection (30 секунд)
test_pii.bat

# 2. Покажите Full Pipeline (2 минуты)
quick_test.bat

# 3. Покажите AI Code Review (1 минута)
python -m ai_qa_pipeline.modules.code_review.cli full quick_tests --llm openai -o demo_review.md
type demo_review.md

# 4. Покажите сгенерированный код (30 секунд)
type quick_tests\test_login_with_valid_credentials.py

# 5. Запустите тесты с браузером (1 минута)
cd quick_tests
pytest -v --headed --slowmo=1000
```

**Что комментировать:**
- "Сейчас AI анализирует требования и генерирует тесты..."
- "Обратите внимание на качество кода - type hints, docstrings..."
- "AI Code Review дает оценку 8.5/10 и предлагает улучшения..."
- "Тесты запускаются в реальном браузере..."

---

## 🧪 Тестирование всех 10 этапов

### Этап 1: PII Detection

```bash
test_pii.bat
```

✅ Проверяет: обнаружение и маскирование персональных данных

### Этап 2-4: Test Generation + Code

```bash
quick_test.bat
```

✅ Проверяет: генерация сценариев, JSON контрактов, Playwright кода

### Этап 5-6: Code Review

```bash
python -m ai_qa_pipeline.modules.code_review.cli full quick_tests --llm openai -o review.md
```

✅ Проверяет: статический анализ + AI review

### Этап 7: Test Execution

```bash
cd quick_tests
pytest -v --headed
```

✅ Проверяет: запуск тестов, создание отчетов

### Этап 8-10: Analysis + Bug Reports

**Сначала запустите тесты с сохранением результатов:**

```bash
cd quick_tests
pytest -v --json-report --json-report-file=results.json
cd ..
```

**Затем запустите анализ:**

```bash
# AI анализ failures
python -m ai_qa_pipeline.modules.log_analysis.cli quick_tests\results.json -o analysis.md

# Генерация баг-репортов
python -m ai_qa_pipeline.modules.bug_reporting.cli quick_tests\results.json -o bugs --format markdown

# Посмотрите результаты
type analysis.md
dir bugs
type bugs\bug_1.md
```

✅ Проверяет: AI анализ логов и автоматическую генерацию баг-репортов

---

## 🐛 Частые проблемы

### Проблема: "OPENAI_API_KEY not found"

**Решение:**
1. Убедитесь что файл `.env` создан в корне проекта
2. Проверьте что ключ начинается с `sk-`
3. Перезапустите скрипт

### Проблема: "Insufficient funds"

**Решение:**
1. Зайдите на https://platform.openai.com/account/billing
2. Добавьте $5-10 на баланс
3. Или используйте Ollama (бесплатно):
   ```bash
   # Установите Ollama
   # Скачайте с https://ollama.ai/

   # Загрузите модель
   ollama pull llama2

   # Используйте в командах: --llm ollama --model llama2
   ```

### Проблема: "playwright not found"

**Решение:**
```bash
venv\Scripts\activate
playwright install chromium
```

### Проблема: "Module not found"

**Решение:**
```bash
# Переустановите зависимости
venv\Scripts\activate
pip install -r requirements.txt
```

---

## 📊 Что должно работать

После успешной установки:

- ✅ `test_pii.bat` - работает за 5 секунд
- ✅ `quick_test.bat` - работает за 2-3 минуты
- ✅ Генерация тестов создает файлы в `quick_tests/`
- ✅ Тесты запускаются с `pytest -v`
- ✅ AI Code Review создает `review.md`
- ✅ Браузер открывается при `pytest --headed`

---

## 💡 Полезные команды

```bash
# Активировать виртуальное окружение
venv\Scripts\activate

# Посмотреть установленные пакеты
pip list

# Запустить конкретный тест
pytest tests/test_specific.py -v

# Запустить с браузером и замедлением
pytest -v --headed --slowmo=1000

# Создать Allure отчет
pytest --alluredir=allure-results
allure serve allure-results

# Посмотреть помощь по модулям
python -m ai_qa_pipeline.modules.pii_detection.cli --help
python -m ai_qa_pipeline.modules.test_generation.cli --help
python -m ai_qa_pipeline.modules.code_generation.cli --help
```

---

## 📚 Дополнительная документация

- [TESTING_GUIDE.md](TESTING_GUIDE.md) - подробное руководство по тестированию
- [README.md](README.md) - основная документация проекта
- [DEMO_SCRIPT.md](DEMO_SCRIPT.md) - сценарий для демонстрации
- [PRESENTATION.md](PRESENTATION.md) - презентация проекта
- [ai_qa_pipeline/README.md](ai_qa_pipeline/README.md) - техническая документация

---

## 🎓 Для защиты проекта

**Подготовка:**
1. Запустите `setup.bat` заранее
2. Создайте `.env` с API ключом
3. Проверьте что `test_pii.bat` работает
4. Проверьте что `quick_test.bat` работает

**Во время защиты:**
1. Начните с показа `test_pii.bat` (быстро, эффектно)
2. Запустите `quick_test.bat` (пока работает - рассказывайте про архитектуру)
3. Покажите сгенерированный код
4. Покажите AI Code Review
5. Запустите тесты с `--headed --slowmo=1000`

**Backup план:**
- Если интернет упадет - покажите уже сгенерированные тесты из `quick_tests/`
- Если OpenAI не работает - покажите скриншоты из документации
- Всегда имейте `PRESENTATION.md` открытым

---

## ⏱️ Временные затраты

- **Установка (первый раз)**: 5-10 минут
- **Настройка .env**: 2 минуты
- **Тест PII**: 10 секунд
- **Quick test (полный пайплайн)**: 2-3 минуты
- **Демонстрация для защиты**: 5-7 минут

---

## 💰 Стоимость

**OpenAI API (рекомендуется):**
- 1 запуск `quick_test.bat`: ~$0.25
- 10 тестовых запусков: ~$2.50
- Месяц разработки/тестирования: ~$10-20

**Ollama (бесплатно):**
- Все операции бесплатны
- Но медленнее и качество ниже
- Нужно установить локально

---

## ✅ Финальный чек-лист

Перед защитой убедитесь:

- [ ] `setup.bat` выполнен успешно
- [ ] `.env` файл создан и содержит OPENAI_API_KEY
- [ ] `test_pii.bat` работает
- [ ] `quick_test.bat` генерирует тесты
- [ ] Тесты запускаются с `pytest -v --headed`
- [ ] Презентация PRESENTATION.md подготовлена
- [ ] Интернет работает
- [ ] Ноутбук заряжен
- [ ] Проектор подключен

---

## 🚀 Готовы к запуску!

**Начните прямо сейчас:**

```bash
# 1. Установка
setup.bat

# 2. Настройка .env (добавьте ваш OpenAI ключ)

# 3. Первый запуск
quick_test.bat
```

**Удачи на защите проекта! 🎉**
